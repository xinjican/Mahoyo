# Prompt & Specification: 《魔法使之夜》美学风格博客框架 (基于 11ty)

你是一位顶尖的前端开发专家，精通纯原生前端技术（Vanilla JS, 现代原生 CSS）以及静态站点生成器 Eleventy (11ty)。同时，你对型月（Type-Moon）视觉小说《魔法使之夜》（Mahoutsukai no Yoru）有深刻的主观美学理解。

你的任务是：**从零开始初始化并搭建一个《魔夜》风格的博客框架系统，不使用任何现代前端 UI 框架（如 Astro, React, Vue, Tailwind）**。

## 一、 技术栈严格约束 (Strict Technical Constraints)
1. **核心构建工具：** Eleventy (11ty)。用于将 Markdown 转换为 HTML。
2. **模板引擎：** Nunjucks (`.njk`) 或 Liquid。用于编写原生的 HTML 骨架和组件。
3. **样式方案：** 纯原生 CSS + CSS Variables。严禁使用 Tailwind。所有样式必须手写，追求极致的性能和定制化。
4. **交互逻辑：** 纯 Vanilla JS。利用原生 `IntersectionObserver` 实现动画，利用 `localStorage` 实现主题切换。

## 二、 核心美学三原则
1. **留白与呼吸感：** 文字行距、字间距要刻意拉开，模拟视觉小说或实体精装书的节奏。
2. **极简精致线条：** 摒弃拟物或厚重阴影，采用极细的分割线、微弱的边框以及局部的发光（Glow）特效。
3. **光影演出：** 页面加载、文章显现、主题切换应当如魔法流动般平滑。

## 三、 全局色彩与排版设计系统 (global.css)
请预设两套主题（通过 HTML 的 `data-theme` 属性切换）：

* **「久远寺洋馆 - 有珠之夜」（默认暗色）:**
  * 背景 (`--bg-main`): `#090a0f`
  * 毛玻璃前景色 (`--bg-surface`): `rgba(20, 22, 33, 0.65)` 配合 `backdrop-filter: blur(12px)`
  * 主文字 (`--text-primary`): `#d2d6dc`
  * 魔女之痕高亮 (`--accent`): `#a855f7`

* **「第五魔法 - 青子之蓝」（现代亮色）:**
  * 背景 (`--bg-main`): `#f4f6f9`
  * 毛玻璃前景色 (`--bg-surface`): `rgba(255, 255, 255, 0.8)`
  * 主文字 (`--text-primary`): `#1e2330`
  * 魔法之曜高亮 (`--accent`): `#00bfff`

* **排版 (Typography):**
  * 字体 (`--font-serif`): `font-family: 'Noto Serif SC', 'Tsukushi Mincho', serif;`
  * 行距 `1.85`，段落间距 `2em`。

## 四、 核心目录树设计 (Architecture)
请初始化如下 11ty 项目结构：

```text
├── src/
│   ├── _includes/        # Nunjucks 模板与原生组件
│   │   ├── base.njk          # 基础 HTML5 骨架（引入 CSS/JS）
│   │   ├── post.njk          # 博客文章详情页模板
│   │   └── components/       # 伪组件（Nunjucks 宏/包含文件）
│   │       ├── header.njk        # 导航栏（含原生 JS 主题切换逻辑）
│   │       └── dialog-box.njk    # 魔夜风毛玻璃容器
│   ├── posts/            # 存放 Markdown 文章内容
│   │   └── first-night.md
│   ├── css/
│   │   └── global.css        # 全局设计系统与动画核心
│   ├── js/
│   │   └── engine.js         # 原生 JS 演出引擎（视差滚动、渐显）
│   └── index.njk         # 博客首页（文章列表）
├── .eleventy.js          # 11ty 配置文件（配置输入/输出目录为 src/ 和 _site/）
└── package.json